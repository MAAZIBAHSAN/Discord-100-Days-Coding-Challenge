//==============(*)==============[-100 Days of code challenge-]==============(*)==============//

//=================-=================(Today Is Day No 4)=================-=================//

//=====(This builds on the previous exercise. Now, you’re not just listing names, you’re using them in a sentence to make a personalized message for each friend.)=====//

//=================-=================(Q12)=================-=================//

let friendNames: string[] = ["Fareed", "Faizan", "Irfan"];

console.log(
  `My friend ${friendNames[0]}, You just need to do your assignments given by mis hina on last sunday   `
);
console.log(
  `My friend ${friendNames[1]}, You only do your homework no: 1 given by mis hina on last sunday`
);
console.log(
  `My friend ${friendNames[2]}, You only record the last Class took by mis hina on last sunday `
);

